Each team gets two columns, one of the columns has a set of symbols, the other is empty. The tribe's job is to match the symbols perfectly. They will have 30 seconds to look at the symbols, then they get covered up, they will have to match it from memory. The team will claim "done". If they match all the symbols they get a point, otherwise they don't get a point. The challenge doesn't end until someone gets a point.
